{
  "name": "dayhelp-static",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs && tailwindcss -i ./src/styles.css -o ./dist/assets/styles.css --minify",
    "dev": "npm run build && serve dist",
    "update:postcodes": "node scripts/update-postcodes.mjs"
  },
  "devDependencies": {
    "@tailwindcss/cli": "^4.2.1",
    "lucide-static": "^1.34.0",
    "serve": "^14.2.5",
    "tailwindcss": "^4.2.1"
  }
}
